# spdlog test configuration file

# Set the default level for all loggers
[global]
level = trace

# Define a specific logger for tests
[logger.test_logger]
level = debug  # Adjust this based on your test verbosity preference
sinks = ["stdout"]
format = "{level} | {source} | {message}"